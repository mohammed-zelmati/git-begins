1- La commande de cloner un repository est :
git clone <URL_du_repository>

2- Les trois commandes de push sont :
   $ git add .
   $ git commit -m "Mon message de commit"
   $ git push origin <nom_de_la_branche> 

3- commande pull est utilisée pour récupérer et intégrer les modifications d’un dépôt distant 
dans votre branche locale :   $ git pull
